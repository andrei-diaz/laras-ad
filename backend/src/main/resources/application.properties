# Application
spring.application.name=laras

# Server
server.port=8080

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://216.238.79.66:5432/carlotum
spring.datasource.username=enarmdev
spring.datasource.password=montemorelos2025

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwt.secret=${JWT_SECRET:laras-secret-key-change-in-production-must-be-at-least-256-bits}
app.jwt.expiration-ms=86400000

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:uploads}

# CORS
app.cors.allowed-origins=http://localhost:3000
